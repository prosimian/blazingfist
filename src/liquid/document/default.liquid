<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    {% if settings.hideSite or hidePage %}
      <meta name="robots" content="noindex">
    {% endif %}

    {% assign siteTitle = 'Your Site Title' %}
    {% if settings.siteTitle %}
      {% assign siteTitle = settings.siteTitle %}
    {% endif %}

    {% assign title = siteTitle %}
    {% if page.title %}
      {% assign title = page.title | append: ' | ' | append: siteTitle  %}
    {% endif %}

    {% assign summary = settings.summary %}
    {% if page.metaDescription %}
      {% assign summary = page.metaDescription %}
    {% endif %}

    <title>{{ title }}</title>
    <meta name="description" content="{{ summary }}"/>

    <link rel="apple-touch-icon" sizes="180x180" href="/favicons/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicons/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicons/favicon-16x16.png">
  </head>

  <body class="flex-full fd-c">

    <a class="skip-link button" href="#main-content">Skip to content</a>

    {% render "partial/header" %}

    <main tabindex="-1" id="main-content" class="flex fd-c f-1">
      {{ page }}
    </main>

    {% render "partial/footer" %}

  </body>

</html>
